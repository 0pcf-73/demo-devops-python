name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov
      
      - name: Set up test database  # 🔹 Agregar esta sección para usar SQLite
        run: |
          echo "DATABASE_URL=sqlite:///db.sqlite3" >> $GITHUB_ENV
      
      - name: Run tests
        env:
          DJANGO_SETTINGS_MODULE: demo.settings  # Asegurar que Django cargue la configuración correcta
        run: |
          python manage.py migrate  # Aplicar migraciones antes de correr pruebas
          pytest --junitxml=report.xml --cov=.
      
      - name: Lint with flake8
        run: |
          pip install flake8
          flake8 . --count --show-source --statistics
      
      - name: Build and push Docker image
        if: github.event_name == 'push'
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker build -t ypalomac/demo-devops-python:latest .
          docker push ypalomac/demo-devops-python:latest
